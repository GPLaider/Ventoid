## Ventoid 0.1.5

- Added selectable `MBR` or `GPT` install mode for Ventoy-style USB creation
- Added Secure Boot preflight verification for the bundled EFI image before install
- Updated the installer UI to show partition-scheme selection and Secure Boot status
- Added tests for GPT layout writing and Secure Boot marker verification
- Kept source-build verification steps documented in the README
